{
  "name": "react-photo-album",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"json-server --watch db.json -p 3001\" \"cd client && yarn start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^3.5.1",
    "json-server": "^0.12.1",
    "npm": "^5.8.0"
  }
}

{
  "name": "react-photo-album",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"json-server --watch db.json --port 3001\" \"cd client && yarn start\"",
    "start-server": "json-server --watch db.json --port 3001"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^3.6.1",
    "json-server": "^0.12.2",
    "npm": "^5.10.0"
  }
}
